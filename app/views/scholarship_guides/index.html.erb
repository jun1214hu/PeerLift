<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"> Peerlift </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/scholarship_guides"> Opportunities <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"> About us </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"> Something else </a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    My Account
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="/auth/google_oauth2">Sign in</a>
                    <a class="dropdown-item" href="/me">My Overview</a>
                    <a class="dropdown-item" href="#">My Scholarships</a>
                    <a class="dropdown-item" href="#">My Task</a>
                    <a class="dropdown-item" href="#">My Files</a>
                    <a class="dropdown-item"> <%= button_to 'Log Out', destroy_path, method: :post if current_user %> </a>
                </div>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
    </div>
</nav>


<p id="notice"><%= notice %> </p>

<div class="container-fluid text-center">
<h1> Scholarship Guides </h1>

<table class="table table-hover col-md-6 offset-md-3">
  <thead>
    <tr>
      <<th>Program name</th>
      <th>Metric box1</th>
      <th>Description short</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scholarship_guides.each do |scholarship_guide| %>
      <tr>
        <td><a href="/scholarship_guides/<%= scholarship_guide.id %>"><%= scholarship_guide.program_name %></a></td>
        <td><%= scholarship_guide.metric_box1 %></td>
        <td><%= scholarship_guide.description_short %></td>
        <td><%= link_to 'Show', scholarship_guide %></td>
        <td><%= link_to 'Edit', edit_scholarship_guide_path(scholarship_guide) %></td>
        <td><%= link_to 'Destroy', scholarship_guide, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= button_to 'Save to my Profile', record_log_path(user_id: current_user.id, scholarship_guide_id: scholarship_guide.id, scholarship_name: scholarship_guide.program_name, saved: 1, completed: 0, previous_save: 0) %></td>
        <td><p id="test">Nothing</p>
      </tr>
    <% end %>
  </tbody>
</table>

</div>

<p><%= @saved %> <p>
<p><%= @scholarship_guides %> <p>
<% @saved.each do |saved| %>
  <%= saved.program_name %>
<% end %>



<br>
